---
- name: Demonstrate yaml fact caching
  # This playbook does nothing but caching data for hosts in the path defined
  # in the ansible.cfg file
  hosts: localhost
  tasks:
    - debug:
        msg: "Caching done"
    

